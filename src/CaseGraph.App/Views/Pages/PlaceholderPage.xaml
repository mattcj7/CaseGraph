<Page x:Class="CaseGraph.App.Views.Pages.PlaceholderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{DynamicResource ContentBackgroundBrush}">
    <Grid Margin="{StaticResource Space24}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding PageTitle}" Style="{StaticResource PageTitleTextStyle}" />

        <TextBlock Grid.Row="1"
                   Text="{Binding Description}"
                   Style="{StaticResource BodyTextStyle}"
                   Margin="{StaticResource SpaceTop12}"
                   TextWrapping="Wrap" />

        <Border Grid.Row="2"
                Margin="{StaticResource SpaceTop16}"
                Style="{StaticResource PlaceholderHintBorderStyle}">
            <TextBlock Text="Placeholder page for T0002. Functional workflows are implemented in subsequent tickets."
                       Style="{StaticResource SecondaryBodyTextStyle}"
                       TextWrapping="Wrap" />
        </Border>
    </Grid>
</Page>
