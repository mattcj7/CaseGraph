<UserControl x:Class="CaseGraph.App.Views.LocationsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource ContentBackgroundBrush}">
    <Grid Margin="{StaticResource Space16}"
          DataContext="{Binding Locations}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="False">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="Locations" Style="{StaticResource PageTitleTextStyle}" />
                <TextBlock Margin="{StaticResource SpaceTop8}"
                           Style="{StaticResource SecondaryBodyTextStyle}"
                           Text="{Binding StatusText}" />
            </StackPanel>

            <StackPanel DockPanel.Dock="Right"
                        Orientation="Horizontal"
                        VerticalAlignment="Center">
                <TextBlock VerticalAlignment="Center"
                           Style="{StaticResource SecondaryBodyTextStyle}"
                           Text="{Binding PageSummaryText}" />
                <Button Margin="{StaticResource SpaceLeft8}"
                        Content="Previous"
                        Command="{Binding PreviousPageCommand}"
                        Style="{StaticResource TopBarButtonStyle}" />
                <Button Margin="{StaticResource SpaceLeft8}"
                        Content="Next"
                        Command="{Binding NextPageCommand}"
                        Style="{StaticResource TopBarButtonStyle}" />
            </StackPanel>
        </DockPanel>

        <Border Grid.Row="1"
                Margin="{StaticResource SpaceTop12}"
                Style="{StaticResource PlaceholderHintBorderStyle}">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="1.3*" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <DatePicker Grid.Column="0"
                                SelectedDate="{Binding FromDateLocal, Mode=TwoWay}"
                                ToolTip="From date (local)" />

                    <DatePicker Grid.Column="2"
                                SelectedDate="{Binding ToDateLocal, Mode=TwoWay}"
                                ToolTip="To date (local)" />

                    <ComboBox Grid.Column="4"
                              ItemsSource="{Binding SubjectFilters}"
                              SelectedItem="{Binding SelectedSubjectFilter, Mode=TwoWay}"
                              DisplayMemberPath="DisplayName"
                              Style="{StaticResource TopBarComboBoxStyle}"
                              ToolTip="Subject filter" />

                    <TextBox Grid.Column="6"
                             Text="{Binding MinAccuracyText, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource TopBarTextBoxStyle}"
                             ToolTip="Minimum accuracy meters" />

                    <TextBox Grid.Column="8"
                             Text="{Binding MaxAccuracyText, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource TopBarTextBoxStyle}"
                             ToolTip="Maximum accuracy meters" />

                    <Button Grid.Column="10"
                            Content="Search"
                            Command="{Binding SearchCommand}"
                            Style="{StaticResource TopBarButtonStyle}" />
                </Grid>

                <WrapPanel Margin="{StaticResource SpaceTop8}">
                    <ComboBox Width="180"
                              ItemsSource="{Binding SourceTypeFilters}"
                              SelectedItem="{Binding SelectedSourceType, Mode=TwoWay}"
                              Style="{StaticResource TopBarComboBoxStyle}"
                              ToolTip="Source type filter" />

                    <Button Margin="{StaticResource SpaceLeft8}"
                            Content="Clear Filters"
                            Command="{Binding ClearFiltersCommand}"
                            Style="{StaticResource TopBarButtonStyle}" />

                    <TextBlock Margin="{StaticResource SpaceLeft12}"
                               VerticalAlignment="Center"
                               Style="{StaticResource SecondaryBodyTextStyle}"
                               Text="{Binding IsLoading, StringFormat=Loading: {0}}" />
                </WrapPanel>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2" Margin="{StaticResource SpaceTop12}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.1*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0"
                      ItemsSource="{Binding Rows}"
                      SelectedItem="{Binding SelectedRow, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      EnableRowVirtualization="True"
                      EnableColumnVirtualization="True"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      ScrollViewer.CanContentScroll="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Timestamp (Local)"
                                        Binding="{Binding TimestampLocalDisplay}"
                                        Width="1.4*" />
                    <DataGridTextColumn Header="Latitude"
                                        Binding="{Binding Latitude, StringFormat={}{0:F6}}"
                                        Width="*" />
                    <DataGridTextColumn Header="Longitude"
                                        Binding="{Binding Longitude, StringFormat={}{0:F6}}"
                                        Width="*" />
                    <DataGridTextColumn Header="Accuracy (m)"
                                        Binding="{Binding AccuracyMeters, TargetNullValue=(none), StringFormat={}{0:F1}}"
                                        Width="0.9*" />
                    <DataGridTextColumn Header="Source Type"
                                        Binding="{Binding SourceType}"
                                        Width="0.8*" />
                    <DataGridTextColumn Header="Source Label"
                                        Binding="{Binding SourceLabel, TargetNullValue=(none)}"
                                        Width="1.2*" />
                    <DataGridTextColumn Header="Subject"
                                        Binding="{Binding SubjectDisplayName, TargetNullValue=(none)}"
                                        Width="1.4*" />
                    <DataGridTemplateColumn Header="Source" Width="1.1*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="View Source"
                                        Command="{Binding DataContext.ViewSourceCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource TopBarButtonStyle}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Citation" Width="1.1*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Copy Citation"
                                        Command="{Binding DataContext.CopyCitationCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource TopBarButtonStyle}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Column="2" Style="{StaticResource PlaceholderHintBorderStyle}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Selected Observation" Style="{StaticResource SectionTitleTextStyle}" />

                        <TextBlock Margin="{StaticResource SpaceTop12}"
                                   Style="{StaticResource SecondaryBodyTextStyle}"
                                   Text="Timestamp (Local)" />
                        <TextBlock Style="{StaticResource BodyTextStyle}"
                                   Text="{Binding SelectedRow.TimestampLocalDisplay, TargetNullValue=(none)}" />

                        <TextBlock Margin="{StaticResource SpaceTop12}"
                                   Style="{StaticResource SecondaryBodyTextStyle}"
                                   Text="Coordinates" />
                        <TextBlock Style="{StaticResource BodyTextStyle}"
                                   Text="{Binding SelectedRow.CoordinatesDisplay, TargetNullValue=(none)}" />

                        <TextBlock Margin="{StaticResource SpaceTop12}"
                                   Style="{StaticResource SecondaryBodyTextStyle}"
                                   Text="Accuracy (m)" />
                        <TextBlock Style="{StaticResource BodyTextStyle}"
                                   Text="{Binding SelectedRow.AccuracyMeters, TargetNullValue=(none), StringFormat={}{0:F1}}" />

                        <TextBlock Margin="{StaticResource SpaceTop12}"
                                   Style="{StaticResource SecondaryBodyTextStyle}"
                                   Text="Subject" />
                        <TextBlock Style="{StaticResource BodyTextStyle}"
                                   Text="{Binding SelectedRow.SubjectDisplayName, TargetNullValue=(none)}"
                                   TextWrapping="Wrap" />

                        <TextBlock Margin="{StaticResource SpaceTop12}"
                                   Style="{StaticResource SecondaryBodyTextStyle}"
                                   Text="Source Locator" />
                        <TextBlock Style="{StaticResource BodyTextStyle}"
                                   Text="{Binding SelectedRow.SourceLocator, TargetNullValue=(none)}"
                                   TextWrapping="Wrap" />

                        <WrapPanel Margin="{StaticResource SpaceTop16}">
                            <Button Content="View Source"
                                    Command="{Binding ViewSourceCommand}"
                                    CommandParameter="{Binding SelectedRow}"
                                    Style="{StaticResource TopBarButtonStyle}" />
                            <Button Margin="{StaticResource SpaceLeft8}"
                                    Content="Copy Citation"
                                    Command="{Binding CopyCitationCommand}"
                                    CommandParameter="{Binding SelectedRow}"
                                    Style="{StaticResource TopBarButtonStyle}" />
                        </WrapPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
