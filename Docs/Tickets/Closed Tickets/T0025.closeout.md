# Ticket Closeout: T0025 - Reports v1 - Target/Global Person Dossier Export (Citations)

## What shipped
- Added a Reports page with a “Create Dossier” workflow for Target/Global Person.
- Implemented dossier compilation with evidence-backed sections and citations (EvidenceItem + SourceLocator).
- Implemented export writer (PDF or print-ready HTML) with safe file naming and output folder selection.
- Export runs off the UI thread with progress and cancellation (JobQueue or async background).
- Logged `ReportExported` audit events with subject, parameters, output path, and correlation id.
- Added deterministic tests for dossier selection rules and audit/export behavior.

## Files changed (high-level)
- UI:
  - Reports page + viewmodel + navigation wiring
  - Export progress/completion UX (open folder / show path)
- Infrastructure:
  - Dossier builder + report model + export service/writer
  - Query plumbing to gather subject identifiers, where-seen summaries, timeline excerpts, and notable message excerpts
  - Audit logging for `ReportExported`
- Tests:
  - Dossier builder tests (sections, date range, citations present)
  - Export/audit integration tests (file created, audit row written)

## Tests run
- `dotnet test` (PASS)

## Follow-ups
- Add calls/locations/media sections to dossier.
- Add batch export for multiple subjects.
- Add “Report templates” (saved section selections + date presets).
- Optional: guardrailed AI-assisted narrative annotations as a future ticket.