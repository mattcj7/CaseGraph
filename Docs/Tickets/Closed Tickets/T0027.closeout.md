# Ticket Closeout: T0027 - Incident Window v1 - offense window comms + geo hits

## What shipped
- Added an Incident Window workspace with wizard-style inputs for time window and optional scene center/radius.
- Implemented results views for comms hits and geo hits within the selected window.
- Added provenance actions for all displayed results: View Source and Copy Citation.
- Added audited IncidentWindowExecuted entries with parameters and result counts.
- Added deterministic tests for time filtering, radius filtering, and audit logging.

## Files changed (high-level)
- UI:
  - Incident Window page + viewmodel + navigation wiring
  - Wizard inputs, results panels, empty states, and provenance actions
- Infrastructure:
  - IncidentWindowQueryService
  - Geo math helper(s) for radius filtering
  - Audit logging for IncidentWindowExecuted
- Tests:
  - Incident window query tests
  - Geo math tests
  - Audit logging coverage

## Tests run
- `dotnet test` (PASS)

## Follow-ups
- Add offline map rendering/tile support for incident window results.
- Add richer ranking/signals (new around offense, comms spikes).
- Add incident packet export improvements.